apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tcdbHelm.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "tcdbHelm.name" . }}
    helm.sh/chart: {{ include "tcdbHelm.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "tcdbHelm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
